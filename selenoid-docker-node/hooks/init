{{ #each cfg.settings.chrome.versions as |version| ~}}
echo pull selenoid/vnc:chrome_{{version}} ...
docker pull selenoid/vnc:chrome_{{version}}
{{/each ~}}

{{#each cfg.settings.firefox.versions as |version| ~}}
echo pull selenoid/vnc:firefox_{{version}} ...
docker pull selenoid/vnc:firefox_{{version}}
{{/each ~}}

# cm selenoid configure -l 10 --vnc --tmpfs 128 --config-dir {{pkg.svc_data_path}}
